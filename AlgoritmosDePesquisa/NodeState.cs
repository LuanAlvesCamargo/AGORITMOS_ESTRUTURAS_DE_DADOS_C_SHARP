namespace Algoritmos.Busca.AEstrela {
    // Define os possíveis estados que um nó pode assumir durante a execução do algoritmo A*.
    
    public enum EstadoNo {
        // O nó ainda não foi considerado para expansão.
        NaoConsiderado = 0,
        // O nó está na lista de nós abertos e pode ser expandido.
        mmary>
        Aberto = 1,
        // O nó já foi processado e não será mais considerado.
        Fechado = 2,
    }
}
